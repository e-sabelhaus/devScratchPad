/**
 * 
 */
package validInteger;

import javax.swing.JOptionPane;
/**
 * @author Eric
 *
 */
class CheckValue {

	public static void main(String[] args) throws IllegalArgumentException {
		boolean isCorrect = false;
		int number = 0;
		do {
		try {
			String s = JOptionPane.showInputDialog(null, "input");
			number = Integer.parseInt(s);
			if (number < 9 && number > 99) {
				throw new IllegalArgumentException("Number cannot be less than 9 or greater than 99!");
			} else {
				isCorrect = true;
			}
			} catch(NumberFormatException ex) {
				throw new IllegalArgumentException("Not a number!: " + number);
			}
			
		} while(isCorrect == false);
				
	}
}
